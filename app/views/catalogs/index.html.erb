<h1>INDEX PAGE</h1>
<link rel="stylesheet" href="/assets/stylesheets/index.css">
<% @catalogs.each do |title| %>
  <%= link_to "#{title.title}", catalog_path(title), class: 'hover-link'%><br><br>

  <%= link_to "Edit", edit_catalog_path(title), class: 'hover-link' %>
  <%= button_to "Destroy", catalog_path(title), method: :delete,
                data: {turbo_confirm: "Are You Sure you want to delete #{title.title}?"} %>
  <br><br>
<% end %>

<!-- Pagination -->
<div>
  <%= pagy_nav(@pagy).html_safe%>
</div>

